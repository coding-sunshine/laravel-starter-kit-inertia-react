; Allow file uploads up to 100MB (required for AI Assistant document uploads).
; HTTP 413 "Payload Too Large" means the server limit is lower than the file size.
upload_max_filesize = 100M
post_max_size = 100M
