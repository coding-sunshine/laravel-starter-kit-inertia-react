# PRD 07: AI Agents, Tools, and Full Verification

## Overview

Add CRM-specific AI tools and MCP tools using the starter kit's existing AI infrastructure, generate pgvector embeddings for contacts, and run the full verification pass across all imported data. **Prerequisites:** PRD 06 must be complete — all CRM data imported, all morph types updated, all relationships remapped.

**CRITICAL: Never touch `sites/default/sqlconf.php`. Never commit without human approval.**

## Technical Context

- **Module path:** `modules/module-crm/` — namespace `Cogneiss\ModuleCrm`
- **Companion docs:** `plan.md` (Step 6), `newdb.md`
- **AI stack (pre-built in starter kit):**
  - `laravel/ai` (wraps `prism-php/prism` + relay) — config at `config/ai.php`
  - `app/Ai/Agents/AssistantAgent.php` — example agent with tools + memory
  - `app/Ai/Tools/UsersIndex.php` — example tool
  - `app/Mcp/Servers/ApiServer.php` — MCP endpoint at `/mcp/api`
  - `app/Mcp/Tools/` — MCP tools for external agents
  - `resources/js/pages/chat/` — React chat UI (already built)
  - `agent_conversations` table — conversation history (pre-built)
  - `credits` + `credit_packs` tables — AI usage tracking (pre-built)
- **pgvector:** Extension installed in PRD 01. `model_embeddings` table pre-built in starter kit (polymorphic, org-scoped). `HasEmbeddings` trait + `SemanticSearchService` ready to use.
- **v3 bot elimination:** v3 used external bot service (`botinabox.online`) + separate `botmysql` DB. v4 replaces with built-in `laravel/ai` agents. No bot tables migrate — relevant prompts extracted and converted.

### Required Environment Variables
Before starting this PRD, verify these are set in `.env`:
- `OPENAI_API_KEY` — Required for AI features (laravel/ai agents, tools, embeddings)
- `THESYS_API_KEY` — Optional: Thesys C1 generative UI (falls back to plain text if unset)
- `DATA_TABLE_AI_MODEL=gpt-4o-mini` — Model for DataTable NLQ queries

**If any required key is missing, ASK the user before proceeding. Do not skip, stub, or use placeholder values.**

## User Stories

### US-001: AI Agents and Tools

**Status:** todo
**Priority:** 1
**Description:** Create 6 CRM-specific AI tool instances for the starter kit's existing `OrgScopedAgent` + `ModuleToolRegistry` infrastructure. Register tools via `CrmModuleServiceProvider::registerAiTools()`. Generate pgvector embeddings using the starter kit's `HasEmbeddings` trait.

**AI Credit System:** AI credit allocation system must be configured with per-plan limits (default 100/period) and BYOK support. See CLAUDE.md "AI Configuration" section for full credit costs per action and AiCreditService API.

**Thesys C1 Generative UI:** All AI agent responses must render via Thesys C1 generative UI (`@thesys/client` React SDK). C1 renders interactive components (ContactCard, PropertyCard, PipelineFunnel, etc.) instead of plain text. Fallback to plain text rendering only when `THESYS_API_KEY` is not set. See CLAUDE.md "Thesys C1 Generative UI" section.

**CRM AI Tools** (at `modules/module-crm/src/Ai/Tools/`):
1. `ContactsSearch.php` — Search contacts by name, email, stage, location. Returns paginated results.
2. `ContactsShow.php` — Get full contact detail with emails, phones, notes, tasks.
3. `ProjectsSearch.php` — Search projects by location, type, price range, developer.
4. `LotsAvailable.php` — Find available lots for a given project with filtering by price/bedrooms.
5. `SalesPipeline.php` — Get sales pipeline status + totals per stage, filterable by org/date range.
6. `CommissionCalc.php` — Calculate expected commissions for a potential sale based on org's commission tier.

All tools must implement `ContextAwareTool` interface and follow the pattern established by `app/Ai/Tools/UsersIndex.php`. Register tools via `ModuleToolRegistry` in `CrmModuleServiceProvider::registerAiTools()` — the starter kit's `OrgScopedAgent` auto-discovers them.

**CRM MCP Tools** (at `modules/module-crm/src/Mcp/Tools/` and registered in `app/Mcp/Tools/`):
- `ContactSearchTool` — search contacts by name/email/phone
- `ProjectLookupTool` — find projects by title/location
- `SaleSummaryTool` — get sale details and commission breakdown
- `LotAvailabilityTool` — check lot availability and pricing
- Plus the same 6 AI tools above, exposed via MCP protocol at `/mcp/api`
- Follow pattern of existing MCP tools in `app/Mcp/Tools/`
- Register tools in MCP server configuration

**Durable Workflows:** Consider using `laravel-workflow/laravel-workflow` for long-running import pipelines (contact import of 9,735 rows, lot import of 121K rows). Waterline UI at `/workflows` for monitoring.

**v3 Bot Knowledge Extraction** (code review, not table migration):
- Review v3 `ai_bot_categories` (11), `ai_bot_boxes` (46), `ai_bot_prompt_commands` (481)
- Convert relevant ones to agent system prompts and AI tool definitions
- No tables migrate — knowledge extracted into code
- ELIMINATE: `botmysql` connection, `SyncUsers` event, external bot service references

**pgvector Embeddings (using starter kit's HasEmbeddings trait):**
- Add `use HasEmbeddings;` to `Contact` model with `toEmbeddableText()` method
- Generate embeddings for 9,735 contacts using `laravel/ai` — stored in starter kit's `model_embeddings` table (polymorphic, org-scoped)
- Use starter kit's `SemanticSearchService` for similarity queries — no custom search service needed
- Enables: "find contacts similar to X", semantic search, AI-powered recommendations

**AI Credit Tracking:**
- `credits` + `credit_packs` tables already exist (starter kit)
- Configure rate limiting with `spatie/laravel-rate-limited-job-middleware`
- Configure per-org overridable settings via Inertia: `/crm/settings/ai` (org admin) or Filament system panel (superadmin defaults)

- [ ] Chat UI loads: `/chat` returns 200
- [ ] 6 CRM AI tools exist: `count(glob(base_path('modules/module-crm/src/Ai/Tools/*.php')))` = 6
- [ ] MCP endpoint accessible: `/mcp/api` returns 200
- [ ] CRM MCP tools registered and accessible via MCP protocol
- [ ] Contact embeddings generated: `DB::table('model_embeddings')->where('embeddable_type', 'contact')->count()` = 9,735
- [ ] Credits tracking tables exist: `Schema::hasTable('credits') && Schema::hasTable('credit_packs')` = true
- [ ] No external bot dependency: `grep -r 'botinabox' modules/module-crm/` returns 0 matches
- [ ] No botmysql connection in config
- [ ] Semantic search works: querying "find contacts similar to [name]" returns relevant results
- [ ] AI credits deducted per tool invocation (check `ai_credit_usage` table has rows after agent interaction)
- [ ] BYOK users bypass credit system (set BYOK key for test user, verify `ai_credit_usage.byok = true` and org pool not decremented)
- [ ] `AiCreditService::canUse()` returns false when org credits exhausted
- [ ] Thesys C1 renders AI responses as interactive components (ContactCard, PropertyCard, etc.) — NOT plain `<p>` text
- [ ] Without `THESYS_API_KEY`: AI responses fall back to plain text gracefully (no crash)
- [ ] 4 CRM MCP tools registered and responding (ContactSearchTool, ProjectLookupTool, SaleSummaryTool, LotAvailabilityTool)
- [ ] MCP endpoint at /mcp/api returns CRM tools in tool list
- [ ] Large import commands (>1000 rows) use laravel-workflow for resumability

### US-002: Full Verification Pass

**Status:** todo
**Priority:** 2
**Description:** Run all verification commands from plan.md's verification checklist. Fix any failures found.

**Run ALL verification commands from prior steps:**

Data integrity:
- `Contact::count()` = 9,735
- `ContactEmail::count()` = 9,730
- `ContactPhone::count()` = 9,601
- `User::count()` = 1,502
- `Project::count()` = 15,481
- `Lot::count()` = 121,360
- `Sale::count()` = 447
- `PropertyReservation::count()` = 120
- `PropertyEnquiry::count()` = 721
- `PropertySearch::count()` = 316
- `Note::count()` = 22,681
- `DB::table('crm_comments')->count()` = 4,259
- `Relationship::count()` = 7,378
- `Partner::count()` = 299
- `Task::count()` = 57
- `DB::table('media')->count()` = 108,510
- `DB::table('project_updates')->count()` = 155,273
- `DB::table('project_favorites')->count()` = 2,172

FK integrity:
- Zero orphaned FKs: `SELECT COUNT(*) FROM contacts c LEFT JOIN organizations o ON c.organization_id=o.id WHERE o.id IS NULL` = 0
- Zero NULL org columns: `collect(['contacts','sales','lots','projects'])->sum(fn($t) => DB::table($t)->whereNull('organization_id')->count())` = 0
- All contacts have `legacy_lead_id`: `Contact::whereNull('legacy_lead_id')->count()` = 0
- All morph types use aliases: `DB::table('media')->where('model_type','LIKE','App\Models\%')->count()` = 0
- Soft-deleted records preserved: `Contact::onlyTrashed()->count()` >= 0

Sync health:
- `php artisan sync:poll` exits with code 0
- `php artisan sync:batch` exits with code 0
- `php artisan sync:status` shows healthy status
- Sync dashboard visible at `/system`

UI health:
- `/crm/contacts` returns 200
- `/crm/projects` returns 200
- `/crm/lots` returns 200
- `/crm/sales` returns 200
- `/crm/reservations` returns 200
- `/admin` returns 200
- `/system` returns 200
- `/dashboard` returns 200
- `/chat` returns 200

- [ ] ALL data count verifications pass (17 entity counts match expected)
- [ ] Zero orphaned foreign keys across all CRM tables
- [ ] Zero NULL organization_id in any CRM table
- [ ] All morph types use aliases (not class names)
- [ ] Sync system healthy (poll, batch, status all pass)
- [ ] All CRM UI pages return 200
- [ ] All Filament panels load correctly
- [ ] Any failures found have been fixed and re-verified
